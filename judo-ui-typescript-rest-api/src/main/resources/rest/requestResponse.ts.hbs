import type { AxiosResponse, AxiosResponseHeaders, RawAxiosResponseHeaders } from 'axios';
import { X_JUDO_COUNT, X_JUDO_COUNT_RECORDS } from './headers';

export type JudoRequestHeaders = Partial<
  Record<string, string> & {
    [X_JUDO_COUNT_RECORDS]?: 'true' | 'false';
  }
>;

export interface JudoRestResponse<T = any> extends AxiosResponse {
  data: T;
  headers: (RawAxiosResponseHeaders | AxiosResponseHeaders) & {
    [X_JUDO_COUNT]?: string;
  };
  status: number;
}
