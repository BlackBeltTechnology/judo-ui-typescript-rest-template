import { {{ joinModelImports dataType }} } from '../model';

export interface {{ restFilterName application.actor dataType }} {
    value: {{# if isEnumType dataType }}{{ dataTyperestParamName dataType }}{{ else }}{{ typescriptType dataType }}{{/ if }};
    operator: {{ typescriptType dataType.operator }};
}